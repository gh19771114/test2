# SMTP 邮件服务器配置
# 请复制此文件为 .env.local 并填写实际的配置值

# SMTP 服务器地址（例如：smtp.gmail.com, smtp.qq.com, smtp.163.com）
SMTP_HOST=smtp.bournmark.jp

# SMTP 端口（通常为 465 或 587）
SMTP_PORT=465

# 是否使用安全连接（465端口通常为true，587端口通常为false）
SMTP_SECURE=true

# SMTP 用户名（通常是您的邮箱地址）
SMTP_USER=info@bournmark.jp

# SMTP 密码（可能是邮箱密码或应用专用密码）
SMTP_PASS=Mark@7745

# 接收邮件的邮箱地址（表单提交后邮件将发送到此地址）
MAIL_TO=info@bournmark.jp
